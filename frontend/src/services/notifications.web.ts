export const registerCompetitionNotifications = async () => {
  // Web push is deliberately not registered through Expo. A future web push
  // implementation must use a separate VAPID subscription and backend contract.
};
