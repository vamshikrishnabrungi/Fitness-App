// TypeScript's bundler resolver requires a base module. Expo selects the
// platform-specific .native.tsx or .web.tsx implementation at runtime.
export { TerritoryMap } from './TerritoryMap.native';
